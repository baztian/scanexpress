# ScanExpress Configuration Sample
#
# Copy this file to config.ini and customize for your setup.
# Each user gets a [user:username] section.
# Each user can have multiple device templates under [user:username:device:name].
#
# Required environment variables (set in /etc/default/scanexpress or systemd):
# - SCANEXPRESS_PAPERLESS_BASE_URL: Base URL of your Paperless-ngx instance
# - SCANEXPRESS_CURRENT_USER: User to use for scanning
#
# Optional environment variables:
# - SCANEXPRESS_SCAN_COMMAND: Default scanner command (default: scanimage)
# - SCANEXPRESS_PAPERLESS_TIMEOUT_SECONDS: Timeout per page during upload (default: 5)
# - SCANEXPRESS_CONFIG_FILE: Override config file path


[user:alice]
paperless_api_token = replace-with-alice-token-abc123

[user:alice:device:brother-color]
device_id = BrotherADS2200:libusb:001:002
scan_command = /opt/scanexpress/scripts/scan_wrapper.sh
scan_timeout_seconds = 30
resolution = 300
mode = 24 bit Color

[user:alice:device:brother-bw]
device_id = BrotherADS2200:libusb:001:002
scan_command = /opt/scanexpress/scripts/scan_wrapper.sh
scan_timeout_seconds = 30
resolution = 200
mode = Gray


[user:bob]
paperless_api_token = replace-with-bob-token-xyz789

[user:bob:device:canon-default]
device_id = Canon:usb:123:456
scan_command = /usr/bin/scanimage
scan_timeout_seconds = 40
resolution = 150
mode = 24 bit Color
